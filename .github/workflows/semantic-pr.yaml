# yamllint disable rule:line-length
# Use template from https://github.com/honestbank/workflows/tree/main/examples/caller-workflows
---
name: "Semantic PR Repository Workflow"
permissions: read-all

on:  # yamllint disable-line rule:truthy
  pull_request:
    types:
      - opened
      - edited
      - synchronize

jobs:
  main:
    name: semantic-pull-request-repository-job
    uses: honestbank/workflows/.github/workflows/general-semantic-pr.yaml@main
    secrets: inherit
